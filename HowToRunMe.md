Simply install the requiments on the file requirements.txt.

```bash
pip install -r requeriments.txt
```

Before having all the requeriments installed run the file main.py an UI using Tkinter should apear.
